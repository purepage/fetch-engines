name: Live Browser Evals

on:
  workflow_dispatch:
    inputs:
      use_cdp_endpoint:
        description: "Use the configured generic CDP endpoint for the Juno probe"
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "0 3 * * *"
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - ".github/workflows/live-browser-evals.yml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "scripts/eval-auto-render.mjs"
      - "scripts/run-live-browser-smoke.mjs"
      - "src/**"
      - "test/live/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/live-browser-evals.yml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "scripts/eval-auto-render.mjs"
      - "scripts/run-live-browser-smoke.mjs"
      - "src/**"
      - "test/live/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  live-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      USE_CDP_ENDPOINT: ${{ github.event_name == 'workflow_dispatch' && inputs.use_cdp_endpoint && secrets.LIVE_CDP_ENDPOINT != '' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm and Node.js
        uses: pnpm/setup@v2
        with:
          version: 11
          runtime: node@22
          cache: true
          install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium and Chrome Stable
        run: pnpm exec playwright install --with-deps chromium chrome

      - name: Build project
        run: pnpm build

      - name: Run general live browser smoke tests
        run: pnpm test:live:smoke -- --testNamePattern='^(?!.*Juno Pure Devotion)'

      - name: Run the Juno probe through an explicitly requested CDP endpoint
        if: ${{ env.USE_CDP_ENDPOINT == 'true' }}
        env:
          LIVE_CDP_ENDPOINT: ${{ secrets.LIVE_CDP_ENDPOINT }}
        run: pnpm test:live:smoke -- --testNamePattern='Juno Pure Devotion'

      - name: Observe the Juno Patchright probe from the GitHub runner
        if: ${{ env.USE_CDP_ENDPOINT != 'true' }}
        continue-on-error: true
        run: pnpm test:live:smoke -- --testNamePattern='Juno Pure Devotion'

  live-auto-render:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm and Node.js
        uses: pnpm/setup@v2
        with:
          version: 11
          runtime: node@22
          cache: true
          install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Build project
        run: pnpm build

      - name: Run live browser auto-render gate
        run: pnpm test:live:auto-render

      - name: Generate live eval report
        if: always()
        run: pnpm eval:auto-render > auto-render-report.json

      - name: Upload live eval report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auto-render-report
          path: auto-render-report.json
